#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: http://prisma-myecomservers.apps-crc.testing:4466
datamodel: datamodel.graphql
#output: /src/generated/

generate:
   - generator: graphql-schema
     output: ./src/generated/prisma.graphql